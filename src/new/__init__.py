"""Workbench — user-driven data workspace (new application).

Self-contained FastAPI backend for the new frontend under ``static/new``.
Uploads user data (timber schema), serves an interactive dashboard with
dimension filters, and provides an OpenAI-compatible AI chat grounded in the
uploaded data.
"""